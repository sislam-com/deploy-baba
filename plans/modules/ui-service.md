# W-UI: ui-service
**Crate:** `services/ui/` | **Status:** DONE
**Coverage floor:** N/A (binary) | **Depends on:** W-CFG, W-CFGT, W-CFGY, W-CFGJ, W-API, W-APIO, W-APIM, W-INFR
**Depended on by:** (none — deployed binary)

---

## W-UI.1 Purpose

The portfolio site and live demonstration of all deploy-baba crates. This is the only
binary in the workspace and is what gets deployed to AWS Lambda. It IS the deployment —
not a demo of it.

Key design: dual-mode entry point (Lambda + local TCP server) with no feature flags.
→ ADR-004

---

## W-UI.2 Route Surface (also the OpenAPI spec)

```
GET  /                         Career timeline + capabilities dual-view — home page (→ W-RSM)
GET  /docs                     RapiDoc interactive API explorer
GET  /health                   {"status":"ok","version":"0.1.0","sha":"abcd1234"}

GET  /api/openapi.json         OpenAPI 3.0 spec (generated by api-openapi crate)
GET  /api/crates               [{name, version, description, traits, doc_url}, ...]
GET  /api/crates/{name}        Full crate metadata + public trait list
GET  /api/stack                Parsed stack.toml rendered as JSON
POST /api/demo/config/parse    Body: {format:"toml"|"yaml"|"json", content:"..."}
                               → Parsed config object or validation errors
POST /api/demo/spec/generate   Body: {fields:[{name,type,required},...], title:"..."}
                               → OpenAPI JSON for a synthetic schema

GET  /api/jobs                 [{slug, company, title, dates, summary, tech_stack}, ...]
GET  /api/jobs/{slug}          Job + detail bullets grouped by category
GET  /api/competencies         [{slug, name, description, icon}, ...]
GET  /api/competencies/{slug}  Competency + cross-referenced evidence items
```

`/api/demo/*` invoke real crates at runtime — not mocks.

---

## W-UI.3 Implementation Notes

### Dual-Mode Entry Point (`main.rs`)
```rust
if std::env::var("AWS_LAMBDA_FUNCTION_NAME").is_ok() {
    lambda_http::run(app).await?;
} else {
    axum::serve(TcpListener::bind("0.0.0.0:3000").await?, app).await?;
}
```

### File Structure
```
services/ui/src/
├── main.rs          # Dual-mode entry point (→ ADR-004)
├── router.rs        # All route registration + OpenAPI spec assembly
├── openapi.rs       # #[derive(OpenApi)] root spec
└── routes/
    ├── resume.rs    # GET /api/resume → JSON handler (home page data, → W-RSM)
    ├── health.rs    # GET /health → JSON
    └── api/
        ├── crates.rs  # crate metadata (hardcoded from workspace)
        ├── stack.rs   # parse stack.toml via config-toml
        └── demo.rs    # live config-parse + spec-generate
```

UI rendering handled by React SPA in `web/` (ADR-019). Lambda serves JSON only.

### Lambda Binary Size (target < 10 MB compressed)
```toml
[profile.release]
lto            = true
codegen-units  = 1
opt-level      = "z"    # optimize for size
strip          = true
```

UI rendering moved to React SPA in web/ (ADR-019). Lambda serves JSON only.
SQLite (rusqlite, bundled) added for resume data — see W-RSM for data model and migration runner.

### Not imported in UI service
`api-graphql` and `api-grpc` — only OpenAPI demo is live. GraphQL/gRPC demonstrated
via static examples in the crate map section (keeps Lambda binary lean).

---

## W-UI.4 Work Items

| ID | Task | Status | Notes |
|----|------|--------|-------|
| W-UI.4.1 | Wire utoipa-rapidoc properly | DONE | Inline HTML approach works fine; loads RapiDoc from CDN. No crate dependency needed. |

---

## W-UI.5 Test Strategy

- No coverage floor (binary crate)
- `just ui` runs clean locally, all routes respond correctly
- `cargo lambda build --release` produces valid Lambda zip (< 10MB)
- Integration: `just infra-verify DOMAIN` checks live routes after deploy
- Manual: `/docs` renders RapiDoc, `/api/demo/config/parse` accepts TOML/YAML/JSON

---

## W-UI.6 Cross-References
- → ADR-004 (dual-mode entry point)
- → ADR-003 (Lambda Function URL)
- → W-CFG, W-CFGT, W-CFGY, W-CFGJ (config parsing in demo endpoint)
- → W-APIO, W-APIM (spec generation in demo endpoint)
- → W-INFR (stack.toml parsing)
- → `plans/cross-cutting/aws-architecture.md`
- → ADR-002 (SQLite on EFS — DB-backed routes)
- → ADR-008 (Cognito auth — AppState refactor, admin routes)
- → ADR-009 (API Gateway for POST /api/contact)
- → ADR-010 (upsert re-seed convention — migration runner)
- → ADR-011 (synchronous email Lambda invoke)
- → ADR-012 (OpenAPI SSOT — spec assembly)
- → ADR-013 (React SPA — frontend delivery)
- → ADR-015 (LLM provider abstraction — W-RST consumer)
- → ADR-019 (SPA deploy pipeline)
